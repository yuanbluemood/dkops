# from django.db import models

# class Join(models.Model):
# 	email = models.EmailField()
# 	timstamp = models.DateTimeField(add_now_add = True, auto_now = False)
# 	updated = models.DateTimeField(add_now_add = False, auto_now = True)

# 	def __unicode__(self):
# 		return "Join"